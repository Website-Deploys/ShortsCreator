"""Data-access adapters: the database connection layer and storage backends.

These are *adapters* implementing capabilities the domain needs. They depend on
the domain contracts (inward), never the other way around.
"""
