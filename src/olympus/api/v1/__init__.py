"""API version 1.

Versioning is explicit from day one (per the API Design spec): breaking changes
ship as a new version while existing clients keep working. All v1 routes are
mounted under the ``/api/v1`` prefix.
"""
